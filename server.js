require('dotenv').config(); const path=require('path'); const express=require('express'); const session=require('express-session'); const SQLiteStore=require('connect-sqlite3')(session); const {one}=require('./lib/db'); const app=express(); const PORT=Number(process.env.PORT||3000),prod=process.env.NODE_ENV==='production'; if(prod&&!process.env.SESSION_SECRET)throw new Error('SESSION_SECRET is required in production.'); app.disable('x-powered-by'); app.use(express.json({limit:'250kb'})); app.use(express.urlencoded({extended:false})); app.use(session({name:'nestlink.sid',store:new SQLiteStore({db:'sessions.db',dir:path.join(__dirname,'data'),createDirIfNotExists:true}),secret:process.env.SESSION_SECRET||'dev-only-change-this-secret',resave:false,saveUninitialized:false,cookie:{httpOnly:true,sameSite:'lax',secure:prod,maxAge:1000*60*60*8}})); app.get('/api/health',(req,res)=>{try{one('SELECT 1 ok');res.json({ok:true,database:'sqlite'});}catch(e){res.status(500).json({ok:false});}}); app.use('/api/auth',require('./routes/auth')); app.use('/api/student',require('./routes/student')); app.use('/api/residence',require('./routes/residence')); app.use(express.static(path.join(__dirname,'public'))); app.get(/^\/(?!api).*/,(_,res)=>res.sendFile(path.join(__dirname,'public','index.html'))); app.use((err,req,res,next)=>{console.error(err);res.status(500).json({error:'Server error. Please try again.'});}); app.listen(PORT,()=>console.log(`UFH NestLink running at http://localhost:${PORT}`));
